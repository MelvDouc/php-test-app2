{% extends "__base.twig" %}

{% block title %}Home{% endblock %}

{% block main %}
  <h1>Home</h1>

  <ul>
  {% for user in users %}
    <li>
      <a href="/profile/{{ user.id }}">{{ user.email }}</a>
    </li>
  {% endfor %}
  </ul>
{% endblock %}